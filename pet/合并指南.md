# 三月七共生式角色发行系统合并指南

本仓库保存功能文件和按顺序应用的 Git 补丁，不包含源项目完整历史。

## 推荐方式：应用补丁

在准备接收功能的 `desktop-march-7th` 工作区中确认工作树干净，然后执行：

```bash
git switch -c feature/character-release-system
git am /path/to/37march/patches/*.patch
```

补丁顺序：

1. `0001-Add-controlled-character-release-system.patch`
2. `0002-Refine-symbiotic-release-workflow-and-companion-safe.patch`

出现冲突时解决文件后运行 `git add <已解决文件>` 和 `git am --continue`；取消应用使用 `git am --abort`。

## 备选方式：完整文件覆盖

`feature-files/` 保持源仓库目录结构。复制到目标仓库后，重点检查以下高冲突文件：

- `electron/main.cjs`
- `electron/companion-store.cjs`
- `electron/preload.cjs`
- `electron/operator-preload.cjs`
- `src/App.tsx`
- `src/components/ReleaseConsole.tsx`
- `src/domain/types.ts`
- `src/operator/api.ts`

`package.json`、`package-lock.json` 和 `shared/skills/march7th-release/` 必须同步。

## 验证

```bash
npm install
npm run check
npm run all
```

预期：115 项自动化测试、TypeScript/Vite 构建、36/36 原型验收和发布审计全部通过。

## 数据兼容

- 桌宠数据：Electron `userData/companion-data.json`。
- 发行工作区：Electron `userData/release-workspace.json`。
- 首次启动会规范化旧数据并过滤历史玩家可见泄漏文案。
- 不要删除现有角色数据；两个工作区由不同文件承载。
