# 三月七桌宠与共生式发行控制台更新文档

更新日期：2026-07-25
适用源仓库：`MihYux/desktop-march-7th`
最新版源提交：`4a9e980`

## 最新版本概述

当前版本将桌宠、区域发行控制台和角色执行 Agent 串成完整的本地发行闭环：

1. 在控制台创建版本任务并上传区域角色共生发行方案。
2. 选择当前区域并查看区域聚合数据和共生式发行 AI。
3. 设置灰度比例后发布；示例发布固定为 100%，仅为演示绕过主动触达频控。
4. 命中灰度的三月七桌宠接收方案，结合玩家授权记忆和当前语境决定执行、延迟或放弃。
5. 三月七只以自然角色语言交流，不展示内部发行术语或决策字段。
6. 控制台用模拟聚合图表展示自然交流、体验意向、拒绝反馈和关系健康。

## 控制台工作流

- **任务总览**：新建版本任务，上传 DOCX/PDF/Markdown/TXT 区域方案。
- **区域与玩家**：维护中国、日本、北美及自定义区域，切换区域并展示区域数据和三个发行 AI。
- **方案发布**：不再生成额外指令，只配置灰度并发布区域方案。
- **示例发布**：固定 100% 灰度，绕过一周一次版本主动触达限制；同意、暂停、勿扰和安全护栏仍生效。
- **效果优化**：无需导入数据，直接展示可追溯的模拟图表和优化结论。

区域工作区保存在 Electron `userData/release-workspace.json`；桌宠角色、消息、记忆和测试接收数据保存在 `userData/companion-data.json`。

## 三月七执行 Skill

内置 `shared/skills/march7th-release/SKILL.md`，版本 1.1.0。核心原则：

- 玩家安全和长期关系优先于发行指标。
- 先回应玩家真实需求，再判断是否适合自然带出版本内容。
- 选择最低必要融入等级；不合适时延迟或放弃。
- 禁止情感施压、虚假记忆、虚假紧迫、付费压力和高频打扰。
- 玩家拒绝后停止主动版本触达，直到玩家主动重新授权。

### 玩家可见内容零泄漏

Skill 与程序输出层共同禁止玩家看到：

- 发行方案、发行目标、发行任务、共生式发行。
- 灰度、触达、频控、指标、实验组、内部审核等后台术语。
- `campaign_id`、`decision`、`release_level`、`preflight_check` 等内部字段。
- 已废弃的“咱刚看到一段……”固定发行模板。

模型回复、确定性主动消息、本地测试投递和历史消息读取统一经过玩家可见内容过滤。启动时迁移历史泄漏文案，避免旧内容重新出现。

## 分层自主记忆

- 每轮聊天保存紧凑情节摘要，不把完整原文直接当作长期记忆。
- 高置信度、非敏感的明确偏好可以形成受控记忆；敏感信息不会进入长期记忆。
- 普通聊天主动检索相关长期记忆和近期情节。
- 发行场景只使用明确授权且与当前话题相关的记忆。
- 关闭记忆或撤销复用权限后，后续上下文立即停止使用。
- 聊天框和 DeepSeek 上下文统一保留最近 10 个完整问答轮次（20 条消息）。

## 稳定性与安全

- 修复桌宠心跳握手误判造成的约 30 秒周期性卡死。
- Windows 透明窗口关闭高负载 GPU 合成路径。
- DeepSeek 请求具备超时、预算、熔断和内容安全处理。
- 玩家 Preload 不暴露发行控制台写权限。
- 区域方案只向命中灰度且通过联系策略的桌宠投递。
- 全局暂停、单任务暂停、拒绝信号和勿扰时间可阻止主动消息。

## 运行方式

```bash
npm install
npm run all
```

也可以分别运行 `npm run start` 和 `npm run operator`。

## 验证结果

- 115 项自动化测试全部通过。
- TypeScript 检查和 Vite 生产构建通过。
- PRD 原型验收 36/36 通过。
- 发布审计通过。

正式线上发行仍需接入服务端玩家数据、线上指标、签名安装包、自动更新和正式合规审核；当前交付是可持久化、可演示完整闭环的本地桌面产品。
